# Verification evidence — 2026-07-13

Verified implementation commit: `2ae6e82` on `dev-6`.

Hosted CI evidence: [run 29239608384](https://github.com/Vivojay/mariana-music-player/actions/runs/29239608384)
completed successfully for head `017fc40` across Windows, Ubuntu, and macOS.

Environment: Windows 11 x64, CPython 3.12.10, Node.js 25.8.2, Electron
43.1.0, the configured FFmpeg/FFprobe/FFplay build from 2025-12-18,
Chromaprint/fpcalc 1.6.0, and rsgain 3.7.

## Passed evidence

- Deterministic Python: 804 passed; five explicitly opt-in live-service tests
  skipped as designed.
- Repository-wide branch-enabled coverage: 91.11%; the 90% gate passed.
- Independent critical-module branch coverage: broadcast 95.0%, credentials
  100.0%, database 100.0%, library 96.2%, library service 96.4%, loudness
  95.1%, media removal 100.0%, paths 100.0%, playback 98.0%, preferences
  100.0%, queue 97.2%, setup 100.0%, sleep timer 100.0%, sources 97.9%, and
  supervisor 96.2%.
- First-run and tool setup tests cover discovery precedence, exact executable
  paths, containing directories, install roots, default automatic setup,
  manual recovery, noninteractive behavior, interruption, invalid archives,
  SHA-256 rejection, archive traversal/symlink rejection, atomic activation,
  persisted configuration, and resume-safe setup ordering.
- Real media-tool acceptance validated FFmpeg, FFprobe, FFplay, fpcalc 1.6.0,
  and rsgain 3.7 from their resolved executable paths.
- A real rsgain scan-only analysis measured
  `res/first_boot_startup_sound.mp3` at -21.25 LUFS with a recommended gain of
  +3.25 dB. SHA-256 and modification time were unchanged after analysis.
- Missing fpcalc is now a typed `UNAVAILABLE` identification result with a
  `tools setup` recovery message; the lyrics command no longer emits a Python
  traceback for that condition.
- Static checks: Python compilation, Ruff, Pyright, version synchronization,
  tracked UTF-8 integrity, documentation consistency, TypeScript, and ESLint
  passed.
- Dependencies: `pip check` passed; fresh pip-audit and npm audit queries
  reported no known vulnerabilities.
- Desktop: eight Vitest tests, the Vite production build, and two native
  Electron/node-pty development scenarios passed.
- The hosted Windows, Ubuntu, and macOS CI matrix completed successfully,
  including dependency installation/audits, branch coverage, critical-package
  coverage, Ruff, Pyright, React tests/build, and native Electron PTY tests.

## CI failures addressed or revalidated

- The previously reported Windows 89.1% coverage result is superseded by the
  91.11% branch-enabled run above.
- Windows-only volume dependencies are imported lazily, so collecting the
  support contract tests does not require pycaw on Linux or macOS.
- The workflow installs FFmpeg on Linux and uses the hosted macOS FFmpeg
  runtime before exercising real media fixtures.

## Unexecuted release gates

- The five public live-service probes were not enabled in this run.
- The eight-hour mixed playback/library/broadcast soak was not repeated.
- Native speaker, Bluetooth/device-loss, sleep/resume, authenticated YouTube,
  credentialed AcoustID, remote Icecast, and manual Linux/macOS acceptance
  remain pending.
- Signed/notarized multi-platform packages and signed update recovery were not
  produced or tested.

The deterministic, static, dependency, real-tool, and development-desktop
gates listed above pass. The remaining gates must not be described as passed,
and the development version remains `0.7.0-dev.3`.

## Packaged YouTube download follow-up

Verified implementation commit: `7da5e9f` on `dev-6`.

- Full Windows Python run with the configured FFmpeg toolchain: 809 passed and
  six skipped (five opt-in public live services and one unavailable local
  fpcalc scenario). Repository branch coverage was 91.23%; every critical
  module passed its independent 95% branch gate.
- Python compilation, Ruff, Pyright, version/text/documentation checks, and
  `pip check` passed. Fresh pip-audit and npm-audit queries reported no known
  vulnerabilities.
- React/Electron checks passed: ESLint, 8 Vitest tests, TypeScript, production
  Vite build, and a fresh PyInstaller/Electron unpacked Windows build.
- Packaged Electron acceptance passed five deterministic scenarios with one
  opt-in live scenario skipped: bundled backend startup, download confirmation
  remaining in the current PTY, setup-once relaunch, interrupted-setup resume,
  and corrupt-setup repair.
- A separate opt-in packaged probe used the exact reported URL
  `https://www.youtube.com/watch?v=aScuQvxmy0E`. It passed by reaching YouTube
  through the Windows trust store, classifying the external 429/bot challenge,
  displaying browser-profile guidance, and proving no second Mariana startup
  occurred. No TLS certificate failure remained.
- A successful credentialed file download was not executed because no browser
  profile was authorized for cookie access. It remains an explicit manual/live
  gate rather than being represented as passed.

## Consistent YouTube browser-profile follow-up

Verified implementation commit: `133e6de` on `dev-6`.

- Search, metadata, direct-link playback, URL handling, and downloads now share
  one atomically persisted browser-profile reference. The deterministic CLI
  acceptance proves `youtube auth status`, `set`, and `clear` apply immediately
  in the same packaged PTY session. `youtube auth test` does not display the
  transient signed media URL.
- Bot/login challenges, rejected profiles, HTTP 429 responses, and certificate
  trust failures have distinct actionable messages. Direct YouTube links use
  local syntax validation and are resolved once by the supervised playback
  path, preventing authentication failures from being mislabeled as invalid
  URLs or invalid searches.
- Full Windows Python run with the configured FFmpeg suite: 823 passed and six
  skipped (five opt-in public live services and one unavailable local fpcalc
  scenario). Repository branch coverage was 91.13%. Independent critical-module
  coverage remained at or above 95% for every enforced module.
- Real FFmpeg acceptance passed for both Opus and MP3 Icecast output, including
  decoding the captured program mix. The fixture now leaves headroom beyond the
  broadcaster's own ten-second connection deadline on loaded CI runners.
- Compilation, Ruff, Pyright, version/text/documentation checks, `pip check`,
  ESLint, TypeScript, eight Vitest tests, and the production Vite build passed.
  Fresh pip-audit and npm-audit queries reported no known vulnerabilities.
- A fresh PyInstaller backend and unpacked Electron application were built.
  Two development Electron/PTY scenarios passed. Five deterministic packaged
  scenarios passed with the credentialed live-download scenario skipped: CLI
  startup, same-session YouTube download dispatch and auth-profile commands,
  setup-once relaunch, interrupted setup resume, and corrupt-state repair.
- No browser cookie access or successful authenticated YouTube extraction was
  attempted without explicit user authorization. YouTube IP rate limits and
  evolving attestation remain external constraints and are not reported as
  successful operations.
