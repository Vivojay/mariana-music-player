# Verification evidence — 2026-07-14

Verified implementation through commit `b56c9a5` on `dev-6`.

Environment: Windows 11 x64, CPython 3.12.10, Node.js 25.8.2, Electron
43.1.0, FFmpeg/FFprobe/FFplay 8.1.2 for managed-tool acceptance, the configured
2025-12-18 FFmpeg build for fixture decoding, Chromaprint/fpcalc 1.6.0, and
rsgain 3.7.

## Passed evidence

- The complete deterministic Python suite passed 975 tests with 17 explicitly
  opt-in or environment-dependent scenarios skipped.
- Aggregate statement/branch coverage was 94.05%. The repository-wide branch
  calculation passed at 90.1%, independently of the aggregate percentage.
- Every critical module independently met the 95% branch gate: albums 96.3%,
  broadcast 95.0%, credentials 100%, database 96.4%, download jobs 95.6%,
  library 95.9%, library service 96.4%, loudness 95.1%, media removal 100%,
  output devices 100%, paths 100%, playback 95.1%, playlists 97.1%,
  preferences 100%, queueing 98.4%, setup 95.0%, sleep timer 100%, sources
  97.9%, station 96.1%, station discovery 98.4%, and supervisor 97.9%.
- Hierarchical queue tests cover migration/rollback, eight-level depth, cycle
  and orphan rejection, cursor stability, atomic groups, undo/redo, dedupe, and
  deterministic sequential, shuffle, priority, artist-fair, smart, and custom
  strategies.
- Playlist and album tests cover CRUD, nested snapshots, M3U/M3U8 import/export,
  explicit YouTube-playlist import, edition separation, multidisc selectors,
  partial-album policy, local-copy preference, MusicBrainz ambiguity, canonical
  YouTube fallback, and release/shuffle/smart/custom playback ordering.
- Download tests cover current-track safety, explicit-only album expansion,
  persisted sequential jobs, destination validation, naming/provenance,
  missing-only selection, progress, pause/resume/cancel, restart recovery, and
  no secondary Mariana session.
- Station tests cover seed eligibility, local/hybrid/online discovery,
  ten-track-ahead replenishment, diversity/deduplication, cancellation, queue
  restoration, paused restart recovery, and bounded/unlimited sessions.
- YouTube chapter tests cover normalization, malformed entries, boundaries,
  seeking, Unicode-aware truncation, terminal prompts, `now`, and desktop
  playback events.
- Ruff, Pyright, Python compilation, version synchronization, UTF-8 integrity,
  documentation synchronization, ESLint, and TypeScript/Vite builds passed.
- `pip check`, `pip-audit`, and `npm audit --audit-level=high` passed with no
  known dependency conflicts or vulnerabilities.
- Vitest passed 12 tests. Native Electron development PTY acceptance passed two
  scenarios.
- A fresh PyInstaller backend was built into the release workflow's exact
  `dist-backend/win-x64` location. A fresh unpacked Electron dev.4 artifact then
  passed five packaged E2E scenarios: bundled backend/version launch,
  same-session download job creation, first setup exactly once, interrupted
  setup resume, and corrupt-state repair. The credentialed live-download case
  remained explicitly skipped because no live URL/profile was supplied.
- The consolidated real-process suite passed 17 tests covering FFmpeg formats,
  FFprobe, FFplay discovery, Chromaprint, captured broadcast decoding, and the
  deterministic short soak.
- All five public live probes produced passing evidence: YouTube search/stream
  decoding, podcast refresh/episode decoding, SomaFM health/decode,
  JavaScript/media-tool discovery, and Chromaprint fingerprinting. The combined
  network run passed four and initially isolated the elevated runner from the
  normal managed-tool data directory; the fingerprint probe then passed when
  explicitly bound to the verified Mariana data directory.

## Pending evidence and release limits

- `npm run rebuild:native` cannot compile node-pty on this workstation because
  Visual Studio C++ Build Tools are not installed. The checked-in dependency's
  Windows x64 prebuild did load successfully in development and packaged PTY
  tests. Hosted Windows CI must still exercise the explicit rebuild step.
- The push-triggered Windows, Ubuntu, and macOS CI matrix has not yet run for
  this commit. Its results must be added after the branch is pushed.
- The credentialed packaged YouTube download, signed/notarized installers, OTA
  update path, macOS/Linux packaged applications, eight-hour soak, remote
  Icecast credentials, and native speaker/Bluetooth/sleep-resume acceptance are
  not claimed as passed.
- `Future Ideas and Issues to Address.md` remains a separate user modification
  and was excluded from every commit and verification diff.

The development version is `0.7.0-dev.4`. This report does not authorize a
stable release or a change to `main`.
