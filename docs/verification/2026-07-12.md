# Verification evidence — 2026-07-12

Verified code commit: `774db35` on `dev-6-reliability`.
`dev-6` was not changed or pushed.

Environment: Windows x64, CPython 3.12.10, Node.js 25.8.2, and the configured
FFmpeg/FFprobe/FFplay build at
`C:\Users\Vivan.Jaiswal\Documents\ffmpeg-2025-12-18-git-78c75d546a-essentials_build\bin`.

## Passed evidence

- Deterministic Python: 541 passed; five opt-in live tests skipped as designed.
- Public live probes: 5 passed—tools, YouTube search/resolve/decode, podcast
  refresh/decode, Chromaprint fingerprinting, and SomaFM health/decode.
- Critical-module branch coverage: broadcast 95.0%, credentials 97.5%, database
  100.0%, library 95.3%, library service 96.4%, loudness 95.0%, paths 100.0%,
  playback 97.6%, queue 98.5%, sleep timer 100.0%, sources 97.9%, supervisor
  96.2%.
- Static checks: Python compilation passed, Ruff passed, Pyright reported zero
  errors/warnings, version metadata matched `0.7.0-dev.2`, and tracked text plus
  documentation integrity checks passed.
- Dependencies: `pip check` passed; pip-audit and npm audit reported no known
  vulnerabilities.
- Desktop: 8 Vitest tests passed; TypeScript and Vite production build passed;
  two real Electron/node-pty Playwright scenarios passed.
- Short soak: 60 seconds, 31 local/HTTP playback cycles, 1,000 library files,
  one authenticated encoded broadcast connection, 1,526,452 broadcast bytes,
  16,347,136 bytes peak RSS growth, and no reported orphan/SQLite/lease failure.

## Blocking or unexecuted release gates

- Repository-wide branch coverage is **83.4%**, below the required 90%. The
  deficit is concentrated in the preserved monolithic legacy REPL and old
  adapters; it is not excluded or hidden from the report.
- The Linux mutmut job and its required 80% score have been configured but were
  not executed on this Windows host.
- The mandatory eight-hour, 10,000-file mixed playback/profiler/broadcast soak
  was not run. The one-minute result does not substitute for it.
- Native speaker, Bluetooth/default-device, suspend/resume, IME/clipboard,
  high-DPI, and macOS/Linux manual acceptance remain pending.
- Credentialed AcoustID, authenticated YouTube, private radio, and remote
  Icecast acceptance remain pending because release test credentials/endpoints
  were not provided.
- Signed/notarized Windows, macOS x64/ARM64, and Linux packages plus N-to-N+1
  OTA recovery were not produced or tested.

Therefore this tree is materially better tested but is **not** an accepted
`0.7.0` release candidate, must remain `0.7.0-dev.2`, and must not advance
`dev-6` until the blocking gates have recorded evidence.
