# Verification evidence — 2026-07-12

Verified code commit: `167be7a` on `dev-6-reliability`.

Environment: Windows 11 x64, CPython 3.12.10, Node.js 25.8.2, Electron
43.1.0, and the configured FFmpeg/FFprobe/FFplay build at
`C:\Users\Vivan.Jaiswal\Documents\ffmpeg-2025-12-18-git-78c75d546a-essentials_build\bin`.

## Passed evidence at this commit

- Deterministic Python: 761 passed; five opt-in live tests skipped as designed.
- Repository-wide branch-enabled coverage: 90.96%; the configured 90% hard
  gate passed without omitting the legacy command loop.
- Independent critical-module branch coverage: broadcast 95.0%, credentials
  100.0%, database 100.0%, library 95.4%, library service 96.4%, loudness
  95.0%, media removal 100.0%, paths 100.0%, playback 97.6%, preferences
  100.0%, queue 97.2%, setup 100.0%, sleep timer 100.0%, sources 97.9%, and
  supervisor 96.2%.
- First-run unit coverage includes fresh/existing installations, completion,
  every step interruption, corrupt state, live/stale/corrupt locks, atomic
  replace failure, optional sample failure/skip, repair, restart, and relaunch.
- Static checks: Python compilation passed, Ruff passed, explicit Pyright
  reported zero errors/warnings, version metadata matched `0.7.0-dev.2`, and
  tracked text plus documentation integrity checks passed.
- Dependencies: `pip check` passed; fresh pip-audit and npm audit runs reported
  no known vulnerabilities.
- Desktop: 8 Vitest tests, TypeScript, ESLint, Vite production build, and two
  development Electron/node-pty scenarios passed.
- Packaging: a fresh PyInstaller Windows x64 backend and unpacked Electron app
  were built. Four packaged tests passed: backend startup, first-run completion
  followed by same-data relaunch without the wizard, interrupted setup resume,
  and corrupt-state repair. The packaged tests used the FFmpeg directory above.
- Process cleanup: after the packaged suite, no process whose executable was
  under `release\win-unpacked` remained running.
- Testing-repository audit: all 61 paths and every externally reachable command
  in `mujcentral/mariana-music-player-testing@2302501` were classified. Its
  unrelated history and obsolete runtime code were not merged.

## Earlier evidence not rerun at this commit

- Five credential-free live probes had passed earlier on this branch: tools,
  YouTube search/resolve/decode, podcast refresh/decode, Chromaprint
  fingerprinting, and SomaFM health/decode. They were not rerun against
  `167be7a`, so they are not counted as current-commit acceptance.
- A prior short soak covered local/HTTP playback, a synthetic library, and an
  authenticated local encoded broadcast. It was not rerun and does not replace
  the release-duration soak.

## Blocking or unexecuted release gates

- The Linux mutmut job and its required 80% score were not executed on this
  Windows host.
- The mandatory eight-hour, 10,000-file mixed playback/profiler/broadcast soak
  was not run at this commit.
- Native speaker, Bluetooth/default-device, suspend/resume, IME/clipboard,
  high-DPI, recycle-bin restoration, and macOS/Linux manual acceptance remain
  pending.
- Credentialed AcoustID, authenticated YouTube, private radio, and remote
  Icecast acceptance remain pending because release credentials/endpoints were
  not provided.
- Signed/notarized Windows, macOS x64/ARM64, and Linux packages plus signed
  N-to-N+1 OTA recovery were not produced or tested. The local Windows package
  was an unpacked development artifact, not a release signature claim.
- A local `node-pty` source rebuild still requires Visual Studio C++ build
  tools. Development and packaged PTY acceptance used the installed prebuilt
  binary.

Therefore the deterministic and packaged gates requested for this integration
pass, but this tree remains `0.7.0-dev.2`; the unexecuted release gates above
must not be described as passed.
