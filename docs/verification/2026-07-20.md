# Source/native acceptance smoke - 2026-07-20

## Scope

This narrow Windows source smoke covered the post-`dev-6` desktop and safety
batch at `26b23e8`. It did not build or inspect a package and is not signed,
multi-OS, speaker-output, or release acceptance.

## Deterministic results

- Focused Mini-player now-playing Vitest: **5 passed**.
- Desktop acceptance Vitest across `App`, Mini-player lifecycle/presentation,
  playback status/seek/projection, and command catalog: **91 passed**.
- Python playback controls/state/projection, desktop catalog, destructive
  command, playlist, and output-target subset: **440 passed, 1 skipped**. The
  skip is the existing Windows environment limitation for symbolic links.
- ESLint: passed with zero warnings.
- Renderer TypeScript check used `--noEmit` and passed.
- Repository hygiene, text integrity, documentation links, and
  `git diff --check`: passed.

The focused source evidence proves:

- Mini-player single-state rendering, control eligibility, identity-bound
  Play/Pause/Previous/Next intents, unavailable-state disabling, read-only
  progress, chapter/current-segment markers, and preferred-region display;
- complete sanitized long identity text remains available while the compact
  title uses its bounded ellipsis styling;
- main-window pointer target calculation, typed seek binding, paused-state
  preservation, preferred-region clamping, stale/blocked/live refusal, safe
  errors, and reconciliation from authoritative projection;
- autocomplete catalog/dropdown selection remains non-executing and cannot
  write to the PTY; and
- playlist mutation binding and lyrics/output no-clobber safety remain green.

## Acceptance still pending

- Native visual inspection of the Mini-player at long titles, display scaling,
  and assistive-technology settings.
- Eligible real-media Mini-player Play/Pause/Previous/Next interaction and
  queue auto-advance in an actual Electron window.
- Physical mouse and touch seeking against real playing media, including the
  paused UI path and preferred-region boundaries.
- Development E2E was not rerun in this pass because its current suite writes
  Playwright results and a screenshot, which this smoke explicitly prohibited.
  Existing committed E2E evidence remains valid but is not fresh evidence for
  this date.
- Packaged lifecycle/E2E, native dependency rebuild, signing, macOS/Linux
  packages, multi-monitor behavior, speaker output, and release soak.

No package or broader native/release readiness claim follows from this report.
