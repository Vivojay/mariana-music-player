# Verification evidence — 2026-07-15

Release-candidate source base: `3c55e16bc7e5b125ed83a0c67fdcdeb7dd5a5432`
on `dev-6`.

Candidate version: `0.7.0-dev.5`. This candidate has not been pushed as a
version-preparation commit, tagged, packaged, or published.

Hosted base evidence: [GitHub Actions run 29388760239](https://github.com/Vivojay/mariana-music-player/actions/runs/29388760239)
completed successfully for `3c55e16` on Windows, Ubuntu, and macOS. Each job
passed the repository branch-coverage gate and its platform-native PTY checks.

Local source-verification environment: Windows 11 x64, CPython 3.12.10,
Node.js 25.8.2, npm, and Electron 43.1.0.

## Passed candidate evidence

- `tools/check_version.py` reported `0.7.0-dev.5`; `version.json`,
  `package.json`, `package-lock.json`, Python reporting, and desktop metadata
  agree.
- Repository hygiene passed across 235 tracked paths.
- UTF-8/text integrity, documentation integrity, Python compilation, Ruff, and
  Pyright passed. Pyright reported zero errors and warnings.
- The deterministic Python suite passed 1,119 tests. Seventeen explicitly live
  or environment-dependent scenarios were skipped.
- Aggregate coverage was 93.95%. Independent repository branch coverage was
  90.0%, and every critical module enforced by `tools/coverage_gate.py` met or
  exceeded 95% branch coverage.
- `pip check` passed. `pip-audit` reported no known vulnerabilities.
- `npm audit --audit-level=high` reported zero vulnerabilities.
- ESLint and TypeScript passed.
- Vitest passed 14 tests across two test files.
- The Vite production frontend build passed. Its existing large-chunk advisory
  remained a non-failing warning.
- Electron development PTY E2E passed two scenarios covering the real Mariana
  PTY, terminal controls, history, resize, themes, and session restart.

## Candidate changes since dev.4

- Added opt-in, privacy-mode-aware Discord Rich Presence.
- Fixed desktop terminal view exit/restart lifecycle and selected-text Ctrl+C.
- Corrected authoritative queue navigation for auto-next.
- Added extractor-backed media-page playback and downloads, centralized URL
  classification on yt-dlp's dedicated extractor registry, and restored the
  `dl-ml` alias.
- Added human-friendly seek syntax, safe relative/end behavior, and validated
  fade command forms while preserving legacy syntax.
- Preserved and repaired preference metadata used by favorite/blocked listings.
- Added project/architecture state documents and `PRODUCT_ROADMAP.md`.
- Restored the repository branch-coverage gate with behavioral seek tests.

## Not performed or claimed

- `dist-backend/` and `release/` were not rebuilt; their existing contents
  predate this candidate and remain stale.
- Packaged Electron E2E, PyInstaller packaging, installer creation, checksums,
  updater metadata, signing, notarization, and OTA tests were not run.
- No Git tag or GitHub Release was created. The public dev.4 release and its
  assets were not modified.
- Credentialed live YouTube, AcoustID, and Icecast scenarios, native speaker
  and Bluetooth acceptance, mutation testing, and the eight-hour soak remain
  outside this source-candidate verification.
- `Future Ideas and Issues to Address.md` remains an author-only local
  modification and was excluded from the candidate diff and every command.

This report authorizes only a source release candidate. It does not authorize
publication, a stable `0.7.0` claim, or advancement of `main`.
