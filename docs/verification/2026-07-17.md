# Verification and prerelease delivery evidence - 2026-07-17

Release-candidate feature base:
`8e0aad630e783fedf47ceb32700c02826a3b52c9` on `dev-6`.

Candidate version: `0.7.0-dev.6`, published as the
[v0.7.0-dev.6 prerelease](https://github.com/Vivojay/mariana-music-player/releases/tag/v0.7.0-dev.6).
The tag resolves to `e27524585b87e00c74ba57fd79a95eb05869b942`.

## Verified source evidence

- [GitHub Actions run 29533372178](https://github.com/Vivojay/mariana-music-player/actions/runs/29533372178)
  passed on Windows, Ubuntu, and macOS for `8e0aad6`. The repository and
  critical-module branch-coverage gates passed on every platform.
- [CodeQL run 29533372168](https://github.com/Vivojay/mariana-music-player/actions/runs/29533372168)
  passed its Python and JavaScript/TypeScript analyses for `8e0aad6`.
- The latest applicable workflow-security run,
  [29504397280](https://github.com/Vivojay/mariana-music-player/actions/runs/29504397280),
  passed actionlint and zizmor. The `8e0aad6` changes did not match that
  workflow's path filters, so no workflow-security run was expected for them.
- The local deterministic Python suite passed 1,252 tests with 17 explicitly
  opt-in or environment-dependent scenarios skipped.
- Aggregate coverage was 93.94%. Repository branch coverage was 90.0%, and
  every module enforced by `tools/coverage_gate.py` met its independent 95%
  branch-coverage requirement.
- Ruff, Pyright, Python compilation, repository hygiene, UTF-8/text integrity,
  documentation integrity, dependency checks, React tests/build, and Electron
  development PTY checks passed in the recorded source-verification cycle.

## Candidate changes since v0.7.0-dev.5

- Added canonical safe playback status for CLI and Electron, improved
  `now`/`progress`, and added a read-only desktop playback status bar.
- Added online search-to-queue commands and command-scoped confirmation bypass
  forms.
- Added conservative indexed-library local-copy matching, a manual inspection
  command, and a one-time terminal-only availability hint without playback
  substitution or remote path disclosure.
- Modernized command help and strengthened repository/workflow security with
  CodeQL, dependency review, actionlint, zizmor, Scorecard, and Dependabot.
- Normalized legacy line endings and stabilized the cross-platform lossy MP3
  broadcast frequency assertion.

## Prerelease delivery evidence

- The public release is non-draft and marked prerelease. It contains only these
  Windows x64 artifacts:

  | Artifact | Size | SHA-256 |
  |---|---:|---|
  | `Mariana-0.7.0-dev.6-windows-x64.exe` | 154,321,048 bytes | `1232ee20fd4c8359d8846708cd96d583c5795571b15781d5668bea58ac3a29d9` |
  | `Mariana-0.7.0-dev.6-windows-x64.exe.blockmap` | 163,379 bytes | `31f106d43cbe3b5db6cfc3d1801e6901c1b4b31529bf1043d0cc6ab9cc833474` |
  | `latest.yml` | 373 bytes | `ba467e5f11af33e2d14388175a0c724630e1d7c1c145561344d9a9c3f1364688` |
  | `SHA256SUMS.txt` | 290 bytes | `337ae9243c615680020e0776ce2231bd5665aa5e0e7dfae842546bd4e4017db9` |

- The PyInstaller backend was rebuilt from this source and matched the backend
  embedded in Electron byte-for-byte. `latest.yml` matched the installer name,
  size, and SHA-512.
- Packaged Electron E2E passed six scenarios: bundled backend launch and
  version reporting, same-session download handling, first setup once and on
  relaunch, interrupted setup resume, and corrupt-state repair. The explicit
  credentialed live-download scenario was skipped because it was not configured.
- The installer is intentionally not Authenticode-signed. The locked Windows
  `node-pty` prebuild was used because Visual Studio C++ Build Tools were not
  available; packaged E2E validated that fallback.

## Remaining manual and stable-release gates

- Dev.6 is an unsigned Windows x64 prerelease only. No Linux, macOS, Windows
  ARM, signing, notarization, OTA, or stable-release claim is authorized.
- Audible speaker output, Bluetooth/default-device switching, sleep/resume,
  credentialed YouTube, AcoustID, and Icecast checks were not performed.
- Credentialed YouTube, AcoustID, and Icecast acceptance, mutation testing, and
  the eight-hour soak remain pending stable-release gates.

This report records verified prerelease delivery evidence. It does not authorize
a stable `0.7.0` claim.

## Post-release Discord Rich Presence source milestone

The following local source commits were created after the published dev.6
target; they are not part of the published dev.6 installer:

- `1f43a0a` hardens lifecycle recovery, retry/dormancy, refresh, shutdown, and
  sanitized failure reporting.
- `24cac9e` configures public Discord Application ID
  `1527444149962277014`, validates it during version/release preflight, and
  asserts that release configuration and `pypresence` remain packaged.
- `b1ff51c` expands privacy, recovery, cancellation, refresh, configuration,
  and coalescing coverage.

Focused Discord presence and CLI tests passed locally. The subsequent full
deterministic suite passed 1,283 tests with 17 environment-dependent cases
skipped and 93.99% aggregate coverage. Repository branch coverage was 90.1%;
the Discord publisher met its independent 95.0% branch gate and the
provider-neutral presence projection reached 100.0%. Repository hygiene,
UTF-8/text integrity, documentation integrity, compilation, Ruff, and Pyright
also passed. The two public-ID/package-contract tests passed explicitly.

The implementation remains off by default, uses local Discord RPC only, and
contains no Discord secret, user token, OAuth credential, or account
credential. Missing or invalid configuration and missing RPC dependencies
become dormant typed failures; transient Discord Desktop and transport loss
retry independently of playback.

Live Discord Desktop acceptance was not performed. Visibility across all four
privacy modes, Discord Activity Privacy behavior, startup-absent recovery,
desktop restart recovery, and shutdown clearing remain manual gates. Backend
and Electron packaging from this post-release source was also not performed.
