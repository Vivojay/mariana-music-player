# Verification evidence - 2026-07-17

Release-candidate feature base:
`8e0aad630e783fedf47ceb32700c02826a3b52c9` on `dev-6`.

Candidate version: `0.7.0-dev.6`. Version and documentation preparation is a
source-only transaction. No dev.6 package, tag, or GitHub Release exists at the
time of this report.

## Verified source evidence

- [GitHub Actions run 29533372178](https://github.com/Vivojay/mariana-music-player/actions/runs/29533372178)
  passed on Windows, Ubuntu, and macOS for `8e0aad6`. The repository and
  critical-module branch-coverage gates passed on every platform.
- [CodeQL run 29533372168](https://github.com/Vivojay/mariana-music-player/actions/runs/29533372168)
  passed its Python and JavaScript/TypeScript analyses for `8e0aad6`.
- The latest applicable workflow-security run,
  [29504397280](https://github.com/Vivojay/mariana-music-player/actions/runs/29504397280),
  passed actionlint and zizmor. The `8e0aad6` changes did not match that
  workflow's path filters, so no workflow-security run was expected for them.
- The local deterministic Python suite passed 1,252 tests with 17 explicitly
  opt-in or environment-dependent scenarios skipped.
- Aggregate coverage was 93.94%. Repository branch coverage was 90.0%, and
  every module enforced by `tools/coverage_gate.py` met its independent 95%
  branch-coverage requirement.
- Ruff, Pyright, Python compilation, repository hygiene, UTF-8/text integrity,
  documentation integrity, dependency checks, React tests/build, and Electron
  development PTY checks passed in the recorded source-verification cycle.

## Candidate changes since v0.7.0-dev.5

- Added canonical safe playback status for CLI and Electron, improved
  `now`/`progress`, and added a read-only desktop playback status bar.
- Added online search-to-queue commands and command-scoped confirmation bypass
  forms.
- Added conservative indexed-library local-copy matching, a manual inspection
  command, and a one-time terminal-only availability hint without playback
  substitution or remote path disclosure.
- Modernized command help and strengthened repository/workflow security with
  CodeQL, dependency review, actionlint, zizmor, Scorecard, and Dependabot.
- Normalized legacy line endings and stabilized the cross-platform lossy MP3
  broadcast frequency assertion.

## Pending packaging and manual gates

- Existing local `dist-backend/` and `release/` output contains dev.5-era
  artifacts. It must be cleaned and rebuilt from the final dev.6 source before
  any package-freshness claim.
- No PyInstaller backend, Electron unpacked app, installer, blockmap,
  `latest.yml`, or checksum manifest has been produced for dev.6.
- Packaged Electron E2E, first-run/OOBE, same-data-directory relaunch,
  interrupted/corrupt setup recovery, speaker output, Bluetooth/default-device
  switching, and packaged online playback/download smoke tests remain pending.
- Dev.6 is limited to a planned unsigned Windows x64 prerelease. No Linux,
  macOS, Windows ARM, signing, notarization, OTA, or stable-release claim is
  authorized.
- Credentialed YouTube, AcoustID, and Icecast acceptance, mutation testing, and
  the eight-hour soak remain pending stable-release gates.

This report records source-candidate evidence only. Packaging and publication
require separate verified transactions.
